/****************************
Filename: NewFile.c
Name: Blakely North
Date Last Edited:
Brief Description:
gcc -Wall -Wextra -O -ansi -pedantic -o NewFile NewFile.c
****************************/
/*printf, scanf*/
#include <stdio.h>
/*rand, srand*/
#include <stdlib.h>
/*time*/
#include <time.h>

#define MAXPLAYERCOUNT 10

int main(void)
{
  int PlayerScore[MAXPLAYERCOUNT][2] = {0};

  return 0;
}
int turn()
{
  int TurnScore[] = {0};
  int DieColor, Result[] = {0};
  int DieToRoll, Turn, i, count, DieCount[] {0};
  srand(time(0));
  rnd(0, 2)
  if (DieCount[RNDNum] = 0)
  {
     
  }
  return 0;
}

int rnd(int low, int high)
{
  int RNDNum;
  RNDNum = rand() % (high - low + 1) + low;
  return 0;
}
