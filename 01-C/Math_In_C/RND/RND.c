/* gcc -O -Wall -Wextra -ansi -pedantic -o hello HelloWorld.c */

#include <time.h>
#include <stdiolib.h>
#include <stdio.h>

int rnd main (void)
{
	int randnum = 0;
	srand(time(NULL)
	
	rnd = rand() % ++6;
	
	printf("Your number is %i\n", rnd)
	
	rnd = rand() % ++6;
	
	printf("Your number is %i\n", rnd)
	
	
	return 0;
}